[build]
  command = "npm run build"
  publish = "dist"

# Client-side routing: every path serves index.html, or a hard refresh on a deep link 404s.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
