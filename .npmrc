# Vite 8 declares an optional peer that npm 10's resolver trips over ("Cannot read
# properties of null (reading 'edgesOut')"). This makes `npm install` work on npm 10 and 11 alike.
legacy-peer-deps=true
